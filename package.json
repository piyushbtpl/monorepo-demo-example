{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "scripts": {
    "fe": "npx lerna run --scope angular-frontend  start",
    "be": "npx lerna run --scope=nest-backend start:dev",
    "both": "npx lerna run --parallel start",
    "run:test": "npm run test"
  }
}
